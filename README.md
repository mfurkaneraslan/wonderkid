# Wonderkid

A mobile-first football career simulation built with Flutter.

## Run locally

```sh
flutter pub get
flutter run
```

The web version is deployed automatically to GitHub Pages after each push to `main`.
